- Feat: Make sure the app uses and stores an admin token for api calls.

- Feat: Should the functions in our endpoint builders use anything from configuration? We have many files which reference commerce endpoints.  We need to understand how everything relates.
