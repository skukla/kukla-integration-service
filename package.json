{
  "name": "kukla-integration-service",
  "version": "0.0.1",
  "private": true,
  "description": "Adobe Commerce integration service for product data export",
  "dependencies": {
    "@adobe/aio-sdk": "^6",
    "csv-writer": "^1.6.0",
    "dotenv": "^16.5.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@openwhisk/wskdebug": "^1.3.0",
    "@vitejs/plugin-legacy": "^6.1.1",
    "eslint": "^8",
    "eslint-plugin-jest": "^27.2.3",
    "jest": "^29",
    "js-yaml": "^4.1.0",
    "postcss-import": "^16.1.0",
    "vite": "^6.3.5"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "clean": "rm -rf dist",
    "test": "jest",
    "deploy": "npm run deploy:full",
    "deploy:full": "npm run clean && npm run build && aio app deploy",
    "deploy:web": "npm run build && aio app deploy --no-actions",
    "deploy:actions": "aio app deploy --no-web-assets"
  },
  "engines": {
    "node": ">=18"
  },
  "aio-app-builder-templates": [
    "@adobe/generator-add-action-generic"
  ]
}
