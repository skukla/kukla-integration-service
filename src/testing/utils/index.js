/**
 * Testing Utils Catalog
 * Low-level testing utility functions
 */

const config = require('./config');
const endpoints = require('./endpoints');
const scenarios = require('./scenarios');

/**
 * Testing utils catalog
 * Organizes low-level testing utilities
 */
module.exports = {
  config,
  endpoints,
  scenarios,
};
